import 'package:flutter/material.dart';

class MealTodoTab extends StatelessWidget {
  const MealTodoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('MealTodoTab'),
    );
  }
}
